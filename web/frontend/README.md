# Frontend (React + Vite)

Quickstart

```powershell
cd web/frontend
npm install
npm run dev
```

Open the dev server (usually http://localhost:5173) and the frontend will call the backend at http://127.0.0.1:5000/api/compile. Ensure the backend is running.
